name: Add rebase label

on:
  pull_request_target:
    types: [synchronize, opened, reopened, labeled, unlabeled]

permissions:
  pull-requests: write
  contents: read

jobs:
  add-rebase-label:
    uses: accel-ppp/.github/.github/workflows/add-rebase-label.yml@T7461-add-reusable-actions
    secrets: inherit
