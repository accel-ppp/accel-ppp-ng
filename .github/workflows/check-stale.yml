name: "Issue and PR stale management"
on:
  schedule:
  - cron: "0 0 * * *"

permissions:
  pull-requests: write
  contents: read

jobs:
  stale:
    uses: accel-ppp/.github/.github/workflows/check-stale.yml@T7461-add-reusable-actions
    secrets: inherit
