---
name: Check pull request message format

on:
  pull_request_target:
    branches:
      - current
    types: [opened, synchronize, edited]

permissions:
  pull-requests: write
  contents: read

jobs:
  check-pr-title:
    uses: accel-ppp/.github/.github/workflows/check-pr-message.yml@T7461-add-reusable-actions
    secrets: inherit
