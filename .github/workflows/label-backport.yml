name: Mergifyio backport

on: [issue_comment]

permissions:
  pull-requests: write
  contents: read

jobs:
  mergifyio-backport:
    uses: accel-ppp/.github/.github/workflows/label-backport.yml@T7461-add-reusable-actions
    secrets: inherit
