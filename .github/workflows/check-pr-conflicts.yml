
name: "PR Conflicts checker"
on:
  pull_request_target:
    types: [synchronize]

permissions:
  pull-requests: write
  contents: read

jobs:
  check-pr-conflict-call:
    uses: accel-ppp/.github/.github/workflows/check-pr-merge-conflict.yml@T7461-add-reusable-actions
    secrets: inherit
